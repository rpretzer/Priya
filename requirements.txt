# Hoopla Coach — Python dependencies
# Core (stdlib only — no extra deps for Ollama and llama.cpp backends)

# AWS Bedrock backend
boto3>=1.34.0

# Anthropic API backend (assisted / direct Claude access)
anthropic>=0.40.0

# Optional: faster PDF text extraction
# pypdf>=4.0.0
