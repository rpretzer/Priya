# Hoopla Coach — Python dependencies

# HTTP server (replaces stdlib http.server)
starlette>=0.46
uvicorn[standard]>=0.34

# Request validation
pydantic>=2.11

# Rate limiting
slowapi>=0.1.9

# AWS Bedrock backend
boto3>=1.34.0

# Anthropic API backend (assisted / direct Claude access)
anthropic>=0.40.0

# Slack bot frontend (pipeline/slack/bot.py)
slack-bolt>=1.18.0

# Terminal CLI frontend (pipeline/cli/chat.py)
# No hard dependencies — uses stdlib urllib + readline
# Optional: richer markdown rendering in terminal
rich>=13.0.0

# Optional: faster PDF text extraction
# pypdf>=4.0.0

# Phase 2 — RAG (retrieval-augmented domain context)
# numpy is required for NumpyVectorStore similarity search
numpy>=1.26.0
# sentence-transformers provides semantic embeddings (best RAG quality)
# Without it, RAG falls back to TF-IDF bag-of-words (no extra deps needed)
# Install: pip install sentence-transformers
# sentence-transformers>=2.7.0
